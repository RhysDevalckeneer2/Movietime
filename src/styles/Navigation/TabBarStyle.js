import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    tabBarStyle: {
            borderRadius: 4,
            backgroundColor: '#FFF',
            height: 60,
            // Reset shadows & border
            borderTopWidth: 0,
            shadowOpacity: 1,
            elevation: 0,
    },
});

export default styles;